/**
 * @author camolezeVitor
 * @description
 * this is only a example file, does not include the true project.
 */

import { NotNull } from "./project/validations/[examples].facade";

class AlunoTest {

    @NotNull()
    public name?: string; 
}