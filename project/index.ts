/**
 * @author camolezeVitor
 * @description
 * this module is responsable for exporting the Gateway, then you can create your own validations decorators
 * then you call the gatway for storaging which validation is selected!
 */
export { ValidationStorageGateway } from "./storage-gateway/gateway/validations-gateway";
export { StorageProtocol } from "./storage-gateway/interfaces/storage.protocol";

